# Smart Attendance - Умная система учёта посещаемости

Мобильное приложение для автоматизации учета посещаемости студентов с возможностью отметки через QR-коды, ручного ввода и генерации отчетов.

## 🚀 Возможности

### Для преподавателей:
- ✅ Создание групп и управление студентами
- ✅ Планирование занятий и генерация QR-кодов
- ✅ Отслеживание посещаемости в реальном времени
- ✅ Генерация статистики и отчетов
- ✅ Экспорт данных в Excel/PDF
- ✅ Уведомления о занятиях

### Для студентов:
- ✅ Сканирование QR-кодов для отметки присутствия
- ✅ Просмотр расписания занятий
- ✅ Отслеживание личной посещаемости
- ✅ Получение уведомлений о занятиях

## 🛠 Технологический стек

- **Framework**: Flutter 3.x
- **State Management**: Provider
- **Database**: 
  - Firebase Firestore (облачная)
  - SQLite (локальная)
- **Authentication**: Firebase Auth
- **QR Codes**: qr_flutter, qr_code_scanner
- **Charts**: fl_chart
- **Localization**: flutter_localizations (русский/казахский)
- **Notifications**: flutter_local_notifications
- **Export**: pdf, excel

## 📱 Поддерживаемые платформы

- ✅ Android
- ✅ iOS
- ✅ Web (базовая поддержка)

## 🚀 Быстрый старт

### Предварительные требования

1. **Flutter SDK** (версия 3.x или новее)
   ```bash
   flutter --version
   ```

2. **Android Studio** или **VS Code** с Flutter плагином

3. **Firebase проект** (см. раздел "Настройка Firebase")

### Установка

1. **Клонируйте проект**
   ```bash
   git clone <repository-url>
   cd smart_attendance
   ```

2. **Установите зависимости**
   ```bash
   flutter pub get
   ```

3. **Настройте Firebase** (см. раздел ниже)

4. **Запустите приложение**
   ```bash
   flutter run
   ```

## 🔥 Настройка Firebase

### Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com/)
2. Нажмите "Создать проект"
3. Введите название: `smart-attendance`
4. Отключите Google Analytics (не обязательно)
5. Нажмите "Создать проект"

### Шаг 2: Настройка Android приложения

1. В консоли Firebase нажмите "Добавить приложение" → Android
2. Введите Package name: `com.smartattendance.app`
3. Введите App nickname: `Smart Attendance Android`
4. Нажмите "Зарегистрировать приложение"
5. **Скачайте файл `google-services.json`**
6. **Замените файл `android/app/google-services.json` на скачанный**

### Шаг 3: Настройка iOS приложения (опционально)

1. В консоли Firebase нажмите "Добавить приложение" → iOS
2. Введите Bundle ID: `com.smartattendance.app`
3. Введите App nickname: `Smart Attendance iOS`
4. Нажмите "Зарегистрировать приложение"
5. **Скачайте файл `GoogleService-Info.plist`**
6. **Поместите файл в папку `ios/Runner/`**

### Шаг 4: Включение сервисов Firebase

#### Authentication
1. В консоли Firebase перейдите в "Authentication"
2. Нажмите "Начать"
3. Перейдите на вкладку "Sign-in method"
4. Включите "Email/Password"

#### Firestore Database
1. В консоли Firebase перейдите в "Firestore Database"
2. Нажмите "Создать базу данных"
3. Выберите "Начать в тестовом режиме"
4. Выберите ближайший регион
5. Нажмите "Готово"

### Шаг 5: Настройка правил безопасности Firestore

Замените правила в Firestore на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи могут читать и писать только свои данные
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Группы доступны только преподавателям
    match /groups/{groupId} {
      allow read, write: if request.auth != null && 
        resource.data.teacherId == request.auth.uid;
    }
    
    // Занятия доступны преподавателям группы
    match /lessons/{lessonId} {
      allow read, write: if request.auth != null && 
        exists(/databases/$(database)/documents/groups/$(resource.data.groupId)) &&
        get(/databases/$(database)/documents/groups/$(resource.data.groupId)).data.teacherId == request.auth.uid;
    }
    
    // Посещаемость доступна преподавателям и студентам группы
    match /attendance/{attendanceId} {
      allow read, write: if request.auth != null && (
        // Преподаватель группы
        exists(/databases/$(database)/documents/lessons/$(resource.data.lessonId)) &&
        exists(/databases/$(database)/documents/groups/$(get(/databases/$(database)/documents/lessons/$(resource.data.lessonId)).data.groupId)) &&
        get(/databases/$(database)/documents/groups/$(get(/databases/$(database)/documents/lessons/$(resource.data.lessonId)).data.groupId)).data.teacherId == request.auth.uid ||
        // Студент группы
        resource.data.studentId == request.auth.uid
      );
    }
  }
}
```

## 📱 Сборка приложения

### Android APK

```bash
# Debug версия
flutter build apk --debug

# Release версия
flutter build apk --release
```

APK файл будет находиться в `build/app/outputs/flutter-apk/`

### iOS (требуется Mac)

```bash
flutter build ios --release
```

## 🧪 Тестирование

```bash
# Запуск тестов
flutter test

# Анализ кода
flutter analyze

# Проверка зависимостей
flutter pub deps
```

## 📁 Структура проекта

```
lib/
├── main.dart                 # Точка входа приложения
├── models/                   # Модели данных
│   ├── user.dart
│   ├── student.dart
│   ├── group.dart
│   ├── lesson.dart
│   ├── attendance.dart
│   └── models.dart
├── screens/                  # Экраны приложения
│   ├── splash_screen.dart
│   ├── login_screen.dart
│   ├── register_screen.dart
│   ├── home_screen.dart
│   ├── group_screen.dart
│   ├── lesson_screen.dart
│   ├── attendance_statistics_screen.dart
│   ├── profile_screen.dart
│   └── screens.dart
├── services/                 # Сервисы
│   ├── firebase_initializer.dart
│   ├── auth_service.dart
│   ├── database_service.dart
│   ├── local_database_service.dart
│   ├── qr_service.dart
│   ├── notification_service.dart
│   └── services.dart
├── providers/                # State management
│   ├── auth_provider.dart
│   ├── locale_provider.dart
│   ├── student_provider.dart
│   ├── group_provider.dart
│   ├── lesson_provider.dart
│   └── providers.dart
├── widgets/                  # Переиспользуемые виджеты
├── utils/                    # Утилиты
└── l10n/                     # Локализация
    ├── app_ru.arb
    └── app_kk.arb
```

## 🌍 Локализация

Приложение поддерживает два языка:
- 🇷🇺 Русский (по умолчанию)
- 🇰🇿 Казахский

Файлы локализации находятся в `lib/l10n/`:
- `app_ru.arb` - русские переводы
- `app_kk.arb` - казахские переводы

## 🔧 Настройка разработки

### VS Code

Рекомендуемые расширения:
- Flutter
- Dart
- Firebase
- GitLens

### Android Studio

Рекомендуемые плагины:
- Flutter
- Dart
- Firebase

## 🐛 Решение проблем

### Ошибка "Firebase not initialized"
- Убедитесь, что файл `google-services.json` находится в `android/app/`
- Проверьте, что в `android/app/build.gradle.kts` добавлен plugin `com.google.gms.google-services`

### Ошибка "Permission denied"
- Проверьте правила безопасности Firestore
- Убедитесь, что пользователь авторизован

### Ошибка сборки Android
```bash
flutter clean
flutter pub get
flutter build apk
```

### Проблемы с зависимостями
```bash
flutter pub deps
flutter pub upgrade
```

## 📋 TODO

- [ ] Реализация QR-сканнера
- [ ] Полная функциональность групп и занятий
- [ ] Графики и статистика
- [ ] Экспорт в Excel/PDF
- [ ] Пуш-уведомления
- [ ] Темная тема
- [ ] Оффлайн режим

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

## 📞 Поддержка

Если у вас есть вопросы или проблемы:

1. Проверьте раздел "Решение проблем"
2. Создайте Issue в репозитории
3. Обратитесь к документации Flutter и Firebase

## 🎯 Roadmap

### Версия 1.0 (Текущая)
- ✅ Базовая аутентификация
- ✅ Управление пользователями
- ✅ Локализация
- ✅ Базовая навигация

### Версия 1.1 (Планируется)
- 🔄 Полная функциональность QR-кодов
- 🔄 Управление группами и студентами
- 🔄 Планирование занятий

### Версия 1.2 (Планируется)
- 📊 Графики и статистика
- 📄 Экспорт отчетов
- 🔔 Система уведомлений

### Версия 2.0 (Будущее)
- 🌙 Темная тема
- 📱 Оффлайн режим
- 🔐 Расширенная безопасность
- 📈 Аналитика

---

**Удачной разработки! 🚀**